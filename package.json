{
  "name": "tagged-table",
  "version": "1.0.0",
  "description": "A function for converting taggled template literals into an array of objects",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch",
    "flow": "flow",
    "clean": "rm -rf dist && mkdir dist",
    "build-module": "babel -d ./dist ./src -s",
    "build": "npm run clean && npm run build-module",
    "prepare": "npm run build"
  },
  "jest": {
    "transform": {
      "^.+\\.jsx?$": "babel-jest"
    }
  },
  "repository": {
    "type": "git",
    "url": "tagged-table"
  },
  "keywords": [
    "JavaScript",
    "TaggedTemplateLiteral",
    "ES6"
  ],
  "author": "Evernote",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "@babel/preset-flow": "^7.0.0",
    "babel-core": "^7.0.0-bridge.0",
    "babel-jest": "^23.6.0",
    "jest": "^23.6.0"
  },
  "dependencies": {
    "flow-bin": "^0.91.0"
  }
}
